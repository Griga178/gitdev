import sys
sys.path.insert(1, '../')

# from my_asyncio.afuncs import main_f
import my_asyncio.afuncs

# from my_asyncio.intsa import *

file_links = [
    '1 links',
    '2 links',
    '3 links',
    '4 links'
    ]

# main_f(file_links)

# ioloop = asyncio.get_event_loop()
# tasks = [
#     ioloop.create_task(gr1()),
#     ioloop.create_task(gr2()),
#     ioloop.create_task(gr3())
# ]
# ioloop.run_until_complete(asyncio.wait(tasks))
# ioloop.close()
